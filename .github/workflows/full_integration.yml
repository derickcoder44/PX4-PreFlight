name: Full Integration Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  # Use the PX4+ROS2 flight test workflow with GUI video recording and camera following
  # TODO: Uncomment once ros-px4-bridge-docker repository is created on GitHub
  # px4-ros-flight-test:
  #   uses: derickcoder44/ros-px4-bridge-docker/.github/workflows/test-flight.yml@main

  # Code quality checks from code-checker submodule
  code-quality-type-check:
    uses: derickcoder44/code-checker/.github/workflows/type_check.yml@main

  code-quality-lint:
    uses: derickcoder44/code-checker/.github/workflows/lint.yml@main

  code-quality-autoreview:
    uses: derickcoder44/code-checker/.github/workflows/autoreview.yml@main
